import React from 'react';
import './App.css';
import {Settings} from "./Settings";

export const App = () => {
    return (
        <div className="App">
            <Settings/>
        </div>
    );
}